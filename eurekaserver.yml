server:
  port: 8070
eureka:
  hostname: "localhost"
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.hostname}:${server.port}/eureka/
